# R can multiple and make bracelets! Do not add before a code as the # will not be accepted
2 == 3
# Crl+ Enter for a single line runs the code
2 >= 3 #always put the greater/ lesser than sign before the = sign
sqrt(4)

#Assignments
x <- 3
x + 3
y <- 10 - 6
y
celsius2Kevin
MaxLengt
mass <- 47.5
mass <- mass * 2.3
mass
age <- 122
age <- age - 20
age
mass < age
mass <= age
mass > age
mass >= age
max.height

#Vectors
vec1 <- c (1, 2, 3, 4, 5)
vec2 <- 1:5
vec3 <- c("dog", "cat", "rabbit")

vec4 <- c("dog", 1, 3)
vec4
typeof(vec4)
typeof(vec1)

vec <- c(TRUE, FALSE, TRUE)
typeof(vec)

rm(vec)

rm(age, mass, vec1)

rm(vec2, vec3, vec4, x, y)

library(gapminder)
library(ggplot2)
library(dplyr)
gapminder












